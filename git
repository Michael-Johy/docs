新建tag
git tag -a tag_name -m 'message'

删除tag
git tag -d tag_name

查看tag | 过滤
git tag |grep 'v0.0.3'


将本地tag标签全部发布到远程仓库
git push origin --tags
特定标签
git push origin tag_name

删除远程标签                                                    
git push -d origin tag_name
